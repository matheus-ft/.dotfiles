# neovim settings live in their own repo (matheus-ft/nvim), cloned straight
# into ~/.config/nvim -- never stowed from here
*/.config/nvim/

# no need to backup last wallpaper used
bg-saved.cfg

# for python configs
.env
__pycache__
.mypy_cache

# macOS junk
.DS_Store
